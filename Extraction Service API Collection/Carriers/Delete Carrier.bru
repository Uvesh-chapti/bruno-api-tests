meta {
  name: Delete Carrier
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/carriers/{{sample_carrier_id}}
  body: none
  auth: inherit
}

headers {
  X-User-ID: {{user_id}}
}

settings {
  encodeUrl: true
}

docs {
  Soft delete a carrier (requires X-User-ID header).
}
