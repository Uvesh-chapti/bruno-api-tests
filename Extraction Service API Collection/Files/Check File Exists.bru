meta {
  name: Check File Exists
  type: http
  seq: 22
}

get {
  url: {{base_url}}/api/extraction-files/{{sample_file_id}}/exists
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Check if a file exists by ID.
}
