meta {
  name: Delete File
  type: http
  seq: 13
}

delete {
  url: {{base_url}}/api/extraction-files/{{sample_file_id}}
  body: none
  auth: inherit
}

headers {
  X-User-ID: {{user_id}}
}

settings {
  encodeUrl: true
}

docs {
  Soft delete a file (requires X-User-ID header).
}
