meta {
  name: Check Invoice Exists
  type: http
  seq: 9
}

get {
  url: {{base_url}}/api/invoices/{{sample_invoice_id}}/exists
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Check if an invoice exists by ID.
}
