meta {
  name: Create Invoice
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/invoices
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "invoiceNumber": "INV-2025-001"
  }
}

settings {
  encodeUrl: true
}

docs {
  Create a new invoice entity (without file pages initially). File pages can be associated separately.
}
