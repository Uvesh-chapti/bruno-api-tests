meta {
  name: Get Extraction File by Invoice ID
  type: http
  seq: 18
}

get {
  url: {{base_url}}/api/invoices/{{sample_invoice_id}}/extraction-file
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Get extraction file information associated with an invoice ID (returns first associated file).
}
