meta {
  name: Get File Page by Invoice Number (Legacy)
  type: http
  seq: 14
}

get {
  url: {{base_url}}/api/invoices/by-number/{{sample_invoice_number}}/page
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Get file page information associated with an invoice number (legacy endpoint, now returns all pages).
}
