meta {
  name: Get File Pages by Invoice Number (Legacy)
  type: http
  seq: 13
}

get {
  url: {{base_url}}/api/invoices/by-number/{{sample_invoice_number}}/pages
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Get file pages associated with an invoice number (legacy endpoint for backward compatibility).
}
