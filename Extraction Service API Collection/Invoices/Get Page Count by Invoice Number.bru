meta {
  name: Get Page Count by Invoice Number
  type: http
  seq: 17
}

get {
  url: {{base_url}}/api/invoices/{{sample_invoice_number}}/page-count
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Get the total number of pages associated with an invoice number.
}
