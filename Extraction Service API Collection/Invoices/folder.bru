meta {
  name: Invoices
  seq: 5
}

auth {
  mode: inherit
}

docs {
  Invoice management endpoints including CRUD operations, lookup by invoice number, and file page access functionality. Updated for 1:many relationship where each invoice can have multiple file pages.
}
