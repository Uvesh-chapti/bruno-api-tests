meta {
  name: Get File Text (All Pages)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/ocr/files/{{sample_file_id}}/text
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Get extracted text for all pages in a file. Returns an array of page texts ordered by page number.
}
